function stringsLength(first, second, third) {

    let sumLengths = first.length + second.length + third.length;
    console.log(sumLengths);
    console.log(Math.floor(sumLengths / 3));

}
stringsLength('chocolate', 'ice cream', 'cake');